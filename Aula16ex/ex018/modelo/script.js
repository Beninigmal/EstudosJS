function adicionar() {
    let numero = document.getElementById('numero')
    let exibir = document.querySelector('select#varicom')

    exibir.innerHTML = 'numero'

    // let cal = document.createElement('option')
    // cal.text = `O valor digitado foi ${numero}`
    // cal.value = `exibir${numero}`

    // exibir.appendChild(cal)

}